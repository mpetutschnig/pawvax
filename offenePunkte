Reminder, der User kann sich einen Reminder bei allen Dokumenten setzen, der Reminder wird aber nicht durch die App erstellt/angezeigt, sondern ein Kalenderauszug den der User sich senden kann, oder direkt im Device eintragen kann.
